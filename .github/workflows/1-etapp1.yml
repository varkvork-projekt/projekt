name: 1-Etapp1 - Nõuded valmis

on:
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write
  actions: write

jobs:
  create_next_issue:
    if: contains(github.event.issue.labels.*.name, 'etapp-1-nõuded')
    runs-on: ubuntu-latest
    
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Congratulate
        uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '🎉 **Etapp 1 tehtud!**\n\nHästi tehtud! Nüüd avaneb Etapp 2 automaatselt. Vaadake Issues lehele!'
            });

      - name: Create Issue 2 - Idee
        uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const issueBody = fs.readFileSync('.github/steps/2-idee.md', 'utf8');
            
            const issue = await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: 'Etapp 2: Mida me täpselt teeme?',
              body: issueBody,
              labels: ['etapp-2-idee']
            });
            
            console.log('✅ Issue 2 created:', issue.data.number);
            
            await github.rest.issues.createComment({
              owner: context.repo.owner,
              repo: context.repo.repo,
              issue_number: issue.data.number,
              body: '💡 **Etapp 2 algas!**\n\nNüüd on aeg valida mida te täpselt teete. Ajurünnak!\n\nKui valmis → märkige ✓ ja sulgege Issue.'
            });
            
      - name: Disable this workflow and enable next
        run: |
          gh workflow disable "${{ github.workflow }}"
          gh workflow enable "2-Etapp2"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
