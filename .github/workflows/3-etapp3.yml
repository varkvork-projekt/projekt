name: 3-Etapp3 - Persoonad valmis

on:
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write
  actions: write

jobs:
  create_next_issue:
    if: contains(github.event.issue.labels.*.name, 'etapp-3-persoonad')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('.github/steps/4-prototyyp.md', 'utf8');
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '🎉 **Etapp 3 tehtud!** Persoonad valmis!'
            });
            const issue = await github.rest.issues.create({
              owner: context.repo.owner, repo: context.repo.repo,
              title: 'Etapp 4: Kuidas see välja näeb?',
              body: body, labels: ['etapp-4-prototüüp']
            });
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: issue.data.number,
              body: '🎨 **Etapp 4 algas!** Nüüd joonistage ja disainige!'
            });
      - run: |
          gh workflow disable "${{ github.workflow }}"
          gh workflow enable "4-Etapp4"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
