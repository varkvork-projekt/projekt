name: 5-Etapp5 - Eelarve valmis

on:
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write
  actions: write

jobs:
  create_next_issue:
    if: contains(github.event.issue.labels.*.name, 'etapp-5-planeerimine')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('.github/steps/6-ehitamine.md', 'utf8');
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '🎉 **Etapp 5 tehtud!** Eelarve valmis!'
            });
            const issue = await github.rest.issues.create({
              owner: context.repo.owner, repo: context.repo.repo,
              title: 'Etapp 6: Nüüd ehitame!',
              body: body, labels: ['etapp-6-ehitamine']
            });
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: issue.data.number,
              body: '🔨 **Etapp 6 algas!** Nüüd saate käed külge! Ehitage!'
            });
      - run: |
          gh workflow disable "${{ github.workflow }}"
          gh workflow enable "6-Etapp6"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
