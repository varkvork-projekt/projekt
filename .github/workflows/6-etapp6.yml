name: 6-Etapp6 - Ehitamine valmis

on:
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write
  actions: write

jobs:
  create_next_issue:
    if: contains(github.event.issue.labels.*.name, 'etapp-6-ehitamine')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('.github/steps/7-testimine.md', 'utf8');
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '🎉 **Etapp 6 tehtud!** Stend ehitatud! 🔨'
            });
            const issue = await github.rest.issues.create({
              owner: context.repo.owner, repo: context.repo.repo,
              title: 'Etapp 7: Kas see töötab hästi?',
              body: body, labels: ['etapp-7-testimine']
            });
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: issue.data.number,
              body: '🧪 **Etapp 7 algas!** Nüüd testimine - kutsuge teisi proovima!'
            });
      - run: |
          gh workflow disable "${{ github.workflow }}"
          gh workflow enable "7-Etapp7 - Testimine valmis"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
