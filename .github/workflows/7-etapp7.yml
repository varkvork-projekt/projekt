name: 7-Etapp7 - Testimine valmis

on:
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write
  actions: write

jobs:
  create_next_issue:
    if: contains(github.event.issue.labels.*.name, 'etapp-7-testimine')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('.github/steps/8-esitlus.md', 'utf8');
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number,
              body: '🎉 **Etapp 7 tehtud!** Testimine läbi!'
            });
            const issue = await github.rest.issues.create({
              owner: context.repo.owner, repo: context.repo.repo,
              title: 'Etapp 8: Viimane! Esitlus ja dokumentatsioon',
              body: body, labels: ['etapp-8-esitlus']
            });
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: issue.data.number,
              body: '🎬 **VIIMANE ETAPP!** Nüüd esitlus ja juhendid. Peaaegu valmis! 🚀'
            });
      - run: |
          gh workflow disable "${{ github.workflow }}"
          gh workflow enable "8-Etapp8 - PROJEKT VALMIS!"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
