name: 8-Etapp8 - PROJEKT VALMIS!

on:
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write
  actions: write

jobs:
  congratulate:
    if: contains(github.event.issue.labels.*.name, 'etapp-8-esitlus')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/github-script@v7
        with:
          script: |
            await github.rest.issues.createComment({
              owner: context.repo.owner, repo: context.repo.repo,
              issue_number: context.issue.number,
              body: `# 🎊 PALJU ÕNNE! PROJEKT ON VALMIS! 🎊

**TE TEGITE SEDA!** 🚀🎉

Olete läbinud kõik 8 etappi:
- ✅ Nõuded ja eesmärgid
- ✅ Idee valimine
- ✅ Persoonad
- ✅ Disain
- ✅ Eelarve
- ✅ Ehitamine
- ✅ Testimine
- ✅ Esitlus

**Võite olla uhked oma töö üle!** 💪

---

*Tagasiside ja küsimused: [@õpetaja](../../discussions)*`
            });
      - run: |
          gh workflow disable "${{ github.workflow }}"
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}