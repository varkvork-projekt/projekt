name: Etapp3

on:
  issues:
    types: [closed]

permissions:
  contents: read
  issues: write

jobs:
  next:
    if: contains(github.event.issue.labels.*.name, 'etapp3')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/github-script@v7
        with:
          script: |
            const fs = require('fs');
            const body = fs.readFileSync('.github/steps/4-prototyyp.md', 'utf8');
            await github.rest.issues.create({
              owner: context.repo.owner,
              repo: context.repo.repo,
              title: 'Etapp 4: Disain',
              body: body,
              labels: ['etapp4']
            });
